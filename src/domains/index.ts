export interface Cusine {
    id: string;
    title: string;
    description: string;
    image: string;
}

export interface RecommendedSection {
    id: string;
    title: string;
    description: string;
    image: string;
    rating: number;
}
